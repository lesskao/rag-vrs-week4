# 🤖 高级 RAG 系统

> 功能强大、高度可扩展的 RAG（Retrieval-Augmented Generation）系统
> 
> 支持多种向量数据库 | 集成先进检索技术 | 生产就绪

[![Python 3.8+](https://img.shields.io/badge/python-3.8+-blue.svg)](https://www.python.org/downloads/)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)

---

## 📖 文档导航

### 🚀 快速导航

| 我想... | 文档链接 |
|---------|----------|
| **5分钟上手** | [快速开始指南](QUICKSTART.md) |
| **运行第一个示例** | [examples/quick_start.py](examples/quick_start.py) |
| **了解项目结构** | [项目结构说明](docs/PROJECT_STRUCTURE.md) |
| **配置环境** | [配置指南](docs/CONFIGURATION_GUIDE.md) |
| **启动 Milvus** | [scripts/start_milvus.bat](scripts/README.md) |
| **性能评测** | [tests/benchmark.py](tests/benchmark.py) |

### 📚 完整文档列表

| 分类 | 文档 | 说明 |
|------|------|------|
| **入门** | [QUICKSTART.md](QUICKSTART.md) | 5分钟快速开始 |
| **配置** | [docs/CONFIGURATION_GUIDE.md](docs/CONFIGURATION_GUIDE.md) | 详细配置和调优 |
| **架构** | [docs/PROJECT_STRUCTURE.md](docs/PROJECT_STRUCTURE.md) | 代码结构说明 |
| **脚本** | [scripts/README.md](scripts/README.md) | Milvus 管理脚本 |
| **脚本** | [scripts/MILVUS_GUIDE.md](scripts/MILVUS_GUIDE.md) | Milvus 完整指南 |

---

## ✨ 核心特性

### 🗄️ 多向量数据库支持
- **Milvus**: 高性能、可扩展的向量数据库，适合生产环境
- **Qdrant**: 现代化的向量搜索引擎，提供丰富的过滤功能
- **Chroma**: 轻量级向量数据库，开发测试首选

### 🚀 高级检索技术

#### 1. 混合检索 (Hybrid Search)
- **向量检索**: 基于语义相似度的检索
- **BM25 检索**: 基于关键词的传统检索
- **RRF 融合**: 使用 Reciprocal Rank Fusion 算法合并两种检索结果

#### 2. 查询变换 (Query Transformation)
- **Multi-Query**: 将单个查询扩展为多个同义查询，提高召回率
- **HyDE**: 生成假设性文档进行检索，对齐语义空间

#### 3. 重排序 (Reranking)
- 使用 DeepSeek 大模型对初检结果进行精排
- 提升 Top-K 结果的相关性

#### 4. 分块策略 (Chunking Strategy)
- **简单分块**: 固定大小的文本分块
- **父子分块**: 索引小块（提高精度），返回大块（提供完整上下文）

### 🤖 大模型集成
- 集成 **DeepSeek API** 作为推理引擎
- 支持查询改写、重排序、答案生成等任务

## ⚡ 快速开始

### 安装

```bash
# 1. 克隆项目
git clone <repository_url>
cd rag-vrs-week4

# 2. 安装依赖
pip install -r requirements.txt

# 3. 运行快速示例（无需配置）
cd examples
python quick_start.py
```

**就这么简单！** 详细步骤请看 → [快速开始指南](QUICKSTART.md)

## 💡 使用示例

### 最简单的例子（5行代码）

```python
from src.vectorstores import ChromaVectorStore
from src.rag_engine import AdvancedRAGEngine
from src.core.models import Document

# 创建引擎
store = ChromaVectorStore("demo")
store.create_collection(768)
rag = AdvancedRAGEngine(store)

# 索引文档
rag.index_documents([Document(id="1", content="Python是编程语言", metadata={})])

# 查询
results = rag.search("Python是什么", top_k=1)
print(results[0].document.content)
```

### 完整示例

```python
from src.vectorstores import ChromaVectorStore
from src.rag_engine import AdvancedRAGEngine
from src.core.models import Document, QueryRequest

# 1. 初始化向量数据库
vector_store = ChromaVectorStore("my_collection")
vector_store.create_collection(dimension=768)

# 2. 创建 RAG 引擎
rag_engine = AdvancedRAGEngine(vector_store)

# 3. 准备文档
documents = [
    Document(
        id="doc1",
        content="深度学习是机器学习的一个分支...",
        metadata={"category": "tech"}
    )
]

# 4. 索引文档
rag_engine.index_documents(documents)

# 5. 查询
request = QueryRequest(
    query="什么是深度学习？",
    top_k=5,
    enable_hybrid=True,
    enable_rerank=True
)

response = rag_engine.query(request)
print(response["answer"])
```

### 示例 2: 切换数据库

只需更换向量数据库实例，业务逻辑无需改动：

```python
# 使用 Qdrant
from src.vectorstores import QdrantVectorStore
vector_store = QdrantVectorStore("my_collection")

# 或使用 Milvus
from src.vectorstores import MilvusVectorStore
vector_store = MilvusVectorStore("my_collection")

# 后续代码完全相同
from src.rag_engine import AdvancedRAGEngine
rag_engine = AdvancedRAGEngine(vector_store)
```

### 示例 3: 父子分块

```python
# 启用父子分块
rag_engine = AdvancedRAGEngine(
    vector_store,
    use_parent_child=True  # 索引小块，返回大块
)

# 索引长文档
long_documents = [...]
rag_engine.index_documents(long_documents)

# 检索时自动返回父块（更完整的上下文）
results = rag_engine.search(query="...")
```

## 📊 性能评测

运行 Benchmark 脚本评测不同数据库的性能：

```bash
cd tests
python benchmark.py
```

评测内容：
- 1000 条文档的索引时间
- 多次查询的平均检索时间
- 重排序前后的结果对比

### 评测结果示例

```
性能对比:
数据库          索引时间(秒)    平均检索时间(秒)
----------------------------------------------------------
Chroma          15.23          0.045
Qdrant          12.87          0.038
Milvus          10.54          0.032

🏆 索引速度最快: Milvus (10.54秒)
🏆 检索速度最快: Milvus (0.032秒)
```

## 🎨 架构设计

### 核心组件

```
┌─────────────────────────────────────────────────────────┐
│                    RAG Engine                           │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │  DeepSeek    │  │  Embedding   │  │   BM25       │  │
│  │   Client     │  │   Manager    │  │  Retriever   │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└────────────────────┬────────────────────────────────────┘
                     │
        ┌────────────┴────────────┐
        │  RAGVectorStore (抽象)  │
        └────────────┬────────────┘
                     │
    ┌────────────────┼────────────────┐
    │                │                │
┌───▼────┐     ┌────▼─────┐    ┌────▼─────┐
│ Milvus │     │  Qdrant  │    │  Chroma  │
└────────┘     └──────────┘    └──────────┘
```

### 数据流

1. **索引阶段**
   - 文档分块 → Embedding 生成 → 入库向量数据库 → BM25 索引

2. **检索阶段**
   - 查询变换（可选）→ 向量检索 + BM25 检索 → RRF 融合 → 重排序 → 返回结果

## 📖 向量数据库选择指南

### Chroma
**优势**:
- 零配置，开箱即用
- 轻量级，适合开发测试
- 本地持久化，无需额外服务

**适用场景**:
- 开发和原型验证
- 小规模应用（< 10万条）
- 单机部署

**不足**:
- 性能相对较低
- 扩展性有限

### Qdrant
**优势**:
- 现代化架构，性能优秀
- 丰富的过滤功能
- 良好的 API 设计
- 支持分布式部署

**适用场景**:
- 中等规模应用（10万-1000万条）
- 需要复杂过滤的场景
- 云原生部署

**不足**:
- 社区相对较小
- 生态不如 Milvus 成熟

### Milvus
**优势**:
- 最高性能，可处理十亿级向量
- 成熟的生产级方案
- 强大的索引算法（HNSW、IVF 等）
- 活跃的社区和生态

**适用场景**:
- 大规模生产环境（> 1000万条）
- 对性能要求极高的场景
- 需要高可用和分布式的场景

**不足**:
- 部署和运维较复杂
- 资源消耗较大

### 性能对比总结

| 特性 | Chroma | Qdrant | Milvus |
|------|--------|--------|--------|
| 索引速度 | ⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ |
| 检索速度 | ⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ |
| 扩展性 | ⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ |
| 易用性 | ⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐ |
| 过滤功能 | ⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐ |
| 生态成熟度 | ⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ |

## 🔧 优化技术详解

### 1. 混合检索 (Hybrid Search)
**解决的痛点**: 单一的向量检索可能遗漏重要关键词

**原理**:
- 向量检索捕获语义相似度
- BM25 检索捕获关键词匹配
- RRF 算法融合两者的排名

**效果**: 召回率提升 15-30%

### 2. Multi-Query
**解决的痛点**: 用户查询表达可能不够准确

**原理**:
- 使用 LLM 将查询改写为多个同义版本
- 并行检索所有版本
- 取结果的并集

**效果**: 召回率提升 10-20%

### 3. HyDE
**解决的痛点**: 查询和文档的语义空间不对齐

**原理**:
- LLM 生成一个"假设性答案"
- 用假设性答案去检索（而不是原始问题）
- 答案和文档的语义更接近

**效果**: 相关性提升 5-15%

### 4. Reranking
**解决的痛点**: 初检的排序可能不够准确

**原理**:
- 初检返回 Top-20
- 使用更强大的模型（DeepSeek）重新打分
- 返回 Top-5

**效果**: Top-5 准确率提升 20-40%

### 5. 父子分块
**解决的痛点**: 小块检索精准但上下文不足，大块上下文足但检索不精准

**原理**:
- 索引时存储小块（提高精度）
- 检索命中后返回其所属的大块（完整上下文）

**效果**: 在保持精度的同时提供更好的上下文

## 📝 API 文档

### RAGVectorStore (抽象接口)

```python
class RAGVectorStore(ABC):
    def batch_upsert(documents: List[Document]) -> bool
    def search(query_embedding, top_k, filters) -> List[SearchResult]
    def delete(doc_ids: List[str]) -> bool
    def get_collection_stats() -> Dict
    def create_collection(dimension: int) -> bool
    def drop_collection() -> bool
```

### AdvancedRAGEngine (核心引擎)

```python
class AdvancedRAGEngine:
    def __init__(vector_store, use_parent_child=False)
    def index_documents(documents, show_progress=True) -> bool
    def search(query, top_k, filters, enable_hybrid, enable_multi_query, enable_hyde) -> List[SearchResult]
    def rerank(query, results, top_k) -> List[SearchResult]
    def query(request: QueryRequest, return_answer=True) -> Dict
```

## 🧪 运行示例

```bash
# 快速启动
cd examples
python quick_start.py

# 使用示例
cd examples
python example_usage.py

# 性能评测
cd tests
python benchmark.py
```

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📄 许可证

MIT License

## 📧 联系方式

如有问题或建议，欢迎联系。

## 📊 项目统计

- **总代码行数**: ~3000+ 行
- **核心模块**: 12 个
- **支持的数据库**: 3 个（Milvus、Qdrant、Chroma）
- **高级技术**: 5 种（混合检索、Multi-Query、HyDE、重排序、父子分块）
- **完整文档**: 5 份详细指南

## 🗂️ 项目结构

```
rag-vrs-week4/
│
├── src/                          # 核心源代码
│   ├── core/                     # 配置和数据模型
│   ├── vectorstores/             # 3种向量数据库实现
│   ├── retrievers/               # 检索和分块策略
│   ├── llm/                      # LLM 和 Embedding
│   └── rag_engine.py             # 主引擎
│
├── examples/                     # 使用示例
│   ├── quick_start.py            # 快速启动
│   └── example_usage.py          # 详细示例
│
├── tests/                        # 测试和评测
│   └── benchmark.py              # 性能评测
│
├── scripts/                      # 辅助脚本
│   ├── start_milvus.bat          # 启动 Milvus
│   ├── stop_milvus.bat           # 停止 Milvus
│   ├── check_milvus_status.bat   # 检查状态
│   ├── test_chroma_simple.py     # Chroma 测试
│   ├── MILVUS_GUIDE.md           # Milvus 指南
│   └── README.md                 # 脚本说明
│
├── docs/                         # 详细文档
│   ├── CONFIGURATION_GUIDE.md    # 配置指南
│   └── PROJECT_STRUCTURE.md      # 结构说明
│
├── config/                       # 配置文件
│   ├── docker-compose.yml        # Milvus 部署配置
│   └── README.md                 # 配置说明
│
├── README.md                     # 本文件
├── QUICKSTART.md                 # 快速开始
├── requirements.txt              # 依赖列表
└── .gitignore                    # Git 忽略
```

## 🎯 学习路径

### 第 1 天：入门
1. ✅ 阅读 [QUICKSTART.md](QUICKSTART.md)
2. ✅ 运行 `examples/quick_start.py`
3. ✅ 理解基本的索引和检索流程

### 第 2-3 天：进阶
4. ✅ 尝试切换不同的向量数据库
5. ✅ 启用高级功能（Multi-Query、重排序）
6. ✅ 运行 `tests/benchmark.py` 性能评测

### 第 4-5 天：深入
7. ✅ 阅读 [docs/PROJECT_STRUCTURE.md](docs/PROJECT_STRUCTURE.md)
8. ✅ 理解各模块的职责和依赖关系
9. ✅ 根据需求调整配置参数

### 第 6-7 天：生产化
10. ✅ 阅读 [docs/CONFIGURATION_GUIDE.md](docs/CONFIGURATION_GUIDE.md)
11. ✅ 选择合适的数据库和配置
12. ✅ 部署到生产环境

## 🌟 技术亮点

### 1. 抽象接口设计
通过 `RAGVectorStore` 抽象类，实现了数据库的无缝切换。

### 2. 模块化架构
清晰的目录结构，每个模块职责单一，易于扩展。

### 3. 配置驱动
所有配置通过环境变量管理，支持多环境部署。

### 4. 性能优化
- 批量操作
- 索引优化
- 混合检索
- 智能缓存

### 5. 用户友好
- 零配置快速启动（Chroma）
- 详细的错误提示
- 完整的文档和示例
- 一键启动脚本（Windows）

## 🎓 适用场景

### ✅ 适用
- 企业知识库搜索
- 智能客服问答
- 文档检索系统
- 代码搜索引擎
- 研究论文检索

### ❌ 不适用
- 实时流式数据处理
- 纯数值计算任务
- 图数据库需求

---

**Happy RAG Building! 🚀**
